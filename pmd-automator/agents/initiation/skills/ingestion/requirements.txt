# PDF → Markdown backends (install at least one)
pymupdf4llm>=0.0.17       # preferred: lightweight, LLM-optimised markdown
# marker-pdf>=0.2.0       # optional: high-fidelity ML-based conversion (GPU recommended)
# pymupdf>=1.24.0         # fallback: plain-text extraction (installed automatically with pymupdf4llm)

# Optional: real-time file-system watching (falls back to polling if absent)
watchdog>=4.0.0
